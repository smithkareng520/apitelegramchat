# 上下文管理升级说明

## 问题

原实现（`app.py` 里 `pre_flight_context_check` / `update_conversation_and_ledger`）
在对话超预算（token 数或消息条数）时，采用**无差别硬删最早整轮对话**的策略：
一旦触发，用户会感觉"突然失忆"——早期有价值的问答和刚发生不久、体积较大的工具
调用结果被同等对待，一起被删掉。同时工具调用结果只有单次 16000 字符的硬截断，
没有区分新旧，旧工具结果会一直占着上下文预算。

## 方案（`context_manager.py`，纯内存 / 零额外模型调用 / 不做摘要）

分层裁剪，从便宜到昂贵、逐级触发，每级都重新检查是否已经够用：

1. **保护窗口**：`system` 消息、以及最近 `KEEP_RECENT_TURNS`（默认 3）轮完整对话
   永远不参与压缩/删除。
2. **第 1 层 · 旧工具结果二次压缩**：保护窗口之外、`role == "tool"` 且内容超过
   `TOOL_RESULT_SOFT_LIMIT`（7500 字符）的消息，压缩为"保留首尾片段 + 长度提示"
   的摘要占位符。`tool_call_id` / `name` 等字段不变，不破坏与 `assistant.tool_calls`
   的配对关系。
3. **第 2 层 · 按轮次裁剪**：仍超预算时整轮删除，但优先删"纯工具轮"（该轮没有
   面向用户的最终文本回复），其次才删有实际问答内容的轮次；且只在已经过第 1 层
   处理的老轮次范围内操作。
4. **第 3 层 · 兜底**：极端情况（单条消息本身就超预算）下退化为只保留
   `system` 消息，与原行为一致，避免死循环。

## 效果对比

同样的裁剪任务下：
- 旧版：不区分工具结果新旧、不区分轮次是否有实际回复，无差别整轮硬删。
- 新版：优先靠压缩旧工具结果省空间（不删任何一次问答），实在不够才删轮次，
  且优先删信息密度最低的纯工具轮；最近几轮和 system prompt 全程不受影响。

## 未覆盖范围（本次明确不做）

- 不生成摘要（不引入额外模型调用），只优化裁剪的顺序和粒度。
- 不做持久化，`conversation_history` 仍是纯内存字典，进程重启会丢失（与升级前
  行为一致，未做改动）。

## 改动文件

- 新增 `src/apitelegramchat/context_manager.py`：分层裁剪核心逻辑，独立可测试。
- 修改 `src/apitelegramchat/app.py`：`pre_flight_context_check` 与
  `update_conversation_and_ledger` 改为调用 `apply_layered_trim`，删除原来两处
  重复的"整轮硬删"代码；对外接口和返回值语义不变。
