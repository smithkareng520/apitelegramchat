# Telegram 工具交互修复说明

本次修复聚焦 Telegram 富消息工具卡片的**可读性、连续性与可验证性**。展示层不会改变传给模型的原始工具结果；模型仍可获得完整输出，而用户界面仅渲染经过裁剪和结构化后的安全视图。

| 范围 | 已完成改动 | 用户可见效果 |
| --- | --- | --- |
| 外部 MCP 返回 | 为地图与位置类 MCP 工具增加 JSON 识别、字段中文化、嵌套列表折叠、链接化与敏感字段屏蔽；非 JSON 回退为带行号的尾部响应面板。 | 不再直接显示难读的原始 JSON，地点、地址、距离和状态等信息以可展开卡片展示。 |
| POI 地点搜索 | 兼容标准 JSON、`pois` 包装对象及多个相邻 JSON 对象拼接的 MCP 文本流；对前 8 个结果生成地点卡片，并为前 3 个有图片的结果展示预览。POI 详情还会展示别名、评分、等级、开放时间、分类和坐标。 | 例如“故宫博物院”会显示地点数量、名称、地址、图片入口、分类编码和 POI ID，而不是连续粘连的原始对象。 |
| 地图编码与定位 | 为地理编码、逆地理编码和 IP 定位识别行政区划、坐标和空结果，提供专用卡片与明确的无结果说明。 | 地址/坐标不再以字段堆栈展示；上游空字段也不会显示为一组空数组。 |
| 路线与距离 | 为驾车、步行、骑行和公交返回生成路线总览；路线步骤、换乘公交、步行距离、方案数量和距离测量统一转换为人类可读单位。 | 长步骤列表折叠后展示，用户优先看到公里数、预计时长、公交线路与关键导航信息。 |
| Bash 工具 | 命令、目录、退出码与输出拆分显示；执行中和完成后都只显示输出的最近 10 行，并使用稳定行号。 | 呈现接近终端/编译器的滚动查看体验，长日志不会撑满聊天页面。 |
| 文件工具 | 创建、替换、插入、按行修改、删除行和撤销后，返回写入后文件的最新 10 行绝对行号快照；执行中同步显示滚动草稿。 | 用户可实时看到当前写入片段，完成后可确认文件末尾实际内容。 |
| 草稿保活 | 所有工具调用每两秒强制重申草稿；强制刷新不会被并发的普通刷新吞掉；成功发送后会重置保活时钟。 | MCP、网络和文件工具无新增文本期间，草稿不再短暂消失后才恢复。 |
| 失败状态 | 超时统一进入友好状态卡片；失败标题优先使用格式化后的摘要，不再直接暴露原始异常文本。 | 错误更简洁，且不会破坏工具组的完成状态。 |

## 修改文件

| 文件 | 作用 |
| --- | --- |
| `src/apitelegramchat/tool_executors.py` | 结构化 MCP 卡片、连续 JSON POI 流解析、地点结果卡片、Bash/文件结果的十行代码面板与敏感字段过滤。 |
| `src/apitelegramchat/search_engine.py` | 文件编辑成功后附带带绝对行号的最新 10 行文件快照。 |
| `src/apitelegramchat/ai_handlers.py` | 流式文件与 Bash 预览、草稿强制保活队列、超时展示和系统提示词。 |

## 本地验证

已完成 Python 语法编译检查，并以模拟的 MCP JSON、12 行 Bash 输出、文件尾部快照和在途强制刷新场景进行了冒烟验证。另使用真实 AMap MCP 返回覆盖了关键词搜索、周边搜索、地理编码、POI 详情、驾车/步行/骑行/公交路线、距离测量与 IP 定位的卡片渲染。交付包不包含 `__pycache__`、`.pyc` 或本次修复过程中的临时补丁/冒烟脚本。
