# 草稿滚动与长 Agent 上下文修复说明

## 结论

本轮针对三个现象完成修复：**新 `draft_id` 首帧不显示 Thinking**、**第二个草稿出现较慢**，以及**长 Agent 回合在会话历史中看起来被整体丢弃**。

Telegram 的同一非零 `draft_id` 才会在原草稿位置平滑更新；切换到新的 `draft_id` 本质上是新的临时预览，无法继承旧草稿的原位动画。因此，新草稿必须自行携带可见的首帧内容。[1] 代码现已在滚动后立即生成包含 `tg-thinking` 的“正在继续生成…”首帧，并继续将同一模型流的后续 delta 追加到该草稿。

对于长 Agent 回合，项目原来的**内轮 Agent 上下文并不会因为草稿滚动而丢失**：每个模型回合使用的 `loop_messages` 在整个工具循环中持续追加 assistant/tool 消息。问题发生在回合完成、写回跨用户请求的 `conversation_history` 后：历史超过 30 条消息或 token 预算时，旧实现直接删除最早的完整 user turn。长工具回合往往包含大量 message，因此会很快触发这一策略，形成“整轮被抛弃”的观感。

现在的策略变为：**先把完整回合压缩成结构化摘要，只有摘要也无法腾出预算时才删除完整回合**。压缩会保留用户目标、已调用工具及最近的阶段性结论，同时始终将 assistant 的 `tool_calls` 与对应 `tool` 消息作为原子组一起移出，避免破坏后续模型请求的工具调用配对。[2]

## 根因与修复

| 问题 | 原因 | 修复 |
|---|---|---|
| 新草稿没有 Thinking | `_replace_with_rollover_remainder` 只写入尾部文本块，不创建 `tg-thinking`。 | 新草稿固定建立 Thinking 块和空文本槽位；即使尾部为空，下一枚 delta 也会可见。 |
| 第二个草稿出现慢 | 旧预览的 `delete_message` 在新 draft 创建前被 `await`；该函数带网络重试，阻塞新首帧。 | 先标记旧草稿失效、生成新 `draft_id` 并强制发送新首帧；旧预览删除改用短超时的后台清理。 |
| 长回合被整体删除 | 消息数上限与 token 预检都从最早完整 user turn 直接删除。 | 首先替换为结构化系统摘要；只有无法继续压缩时才执行完整回合删除。 |
| 工具调用上下文配对风险 | 逐条删除 assistant/tool 消息会令下一模型请求包含未配对的 `tool_calls`。 | 按 user turn 处理；每一回合内部的 assistant `tool_calls` 与所有 `tool` 结果同进同出。 |

## 草稿滚动后的新时序

| 顺序 | 操作 | 用户可见结果 |
|---:|---|---|
| 1 | 在完整结构边界前永久化旧段 | 已完成内容成为正常富消息。 |
| 2 | 标记旧 `draft_id` 失效 | 旧草稿不会再收到迟到的刷新。 |
| 3 | 生成并登记新 `draft_id` | 新草稿可立即通过活跃状态校验。 |
| 4 | 写入 `正在继续生成…` 与未提交尾部 | 新草稿首帧不再为空。 |
| 5 | 当前 `flush(force=True)` 立即发送新草稿 | 用户先看到新 Thinking，再等待新的模型 delta。 |
| 6 | 后台快速清理旧预览 | 删除失败不会阻塞新草稿；旧草稿会自然过期。 |

> `sendRichMessageDraft` 是临时预览，最终仍由 `sendRichMessage` 持久化。相同的非零 `draft_id` 可更新同一预览；新 `draft_id` 则是新的预览实体。[1]

## 长回合上下文策略

本实现没有在 Agent 正在运行时压缩 `loop_messages`，因此一轮内的工具结果和推理链路仍完整可用。压缩只在下一次请求进入前的预检，以及当前回合结束写入持久会话历史后执行。

| 优先级 | 策略 | 保留内容 | 适用条件 |
|---:|---|---|---|
| 1 | 保持活跃内轮上下文 | 当前 `loop_messages` 的所有 assistant/tool 配对 | Agent 当前正在执行。 |
| 2 | 压缩最旧完整回合 | 用户目标、工具名轨迹、阶段性结论 | token 或消息条数接近预算。 |
| 3 | 合并历史摘要 | 多个旧回合的结构化摘要 | 已存在早期摘要，继续压缩时。 |
| 4 | 删除最旧完整回合 | 不保留原文，但不破坏工具配对 | 摘要也不能满足硬 token 预算。 |

这种“压缩优先、完整回合兜底删除”的分层方式符合长时 Agent 的上下文管理实践：上下文压缩应选择性删减、折叠或摘要较旧部分；含工具调用的 assistant 消息和对应工具结果应作为不可拆分的逻辑组处理。[2] Anthropic 对长时 Agent 的工程实践也强调，单纯的窗口压缩不足以保障连续性，应留下可供下一窗口使用的结构化进度和关键工作状态。[3]

## 回归验证

已通过以下检查：

```bash
python3 -m py_compile \
  src/apitelegramchat/ai_handlers.py \
  src/apitelegramchat/app.py \
  validate_draft_rollover.py \
  validate_draft_frontend_flow.py \
  validate_context_compaction.py

python3 validate_draft_rollover.py
python3 validate_draft_frontend_flow.py
python3 validate_context_compaction.py
```

| 测试 | 结果 | 覆盖重点 |
|---|---|---|
| `validate_draft_rollover.py` | 通过 | 新草稿包含 Thinking、完整结构滚动、fallback 内容无丢失、旧预览清理异步化。 |
| `validate_draft_frontend_flow.py` | 通过 | 初始草稿早于耗时准备阶段出现，准备状态可见，工具详情预算受控。 |
| `validate_context_compaction.py` | 通过 | 工具回合压缩为摘要而非丢弃；预检会保留目标、工具轨迹和结论。 |

## 参考资料

[1]: https://core.telegram.org/bots/api#sendrichmessagedraft "Telegram Bot API — sendRichMessageDraft"
[2]: https://learn.microsoft.com/en-us/agent-framework/concepts/agents/conversations/compaction "Microsoft Learn — Compaction"
[3]: https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents "Anthropic — Effective harnesses for long-running agents"
