# 草稿滚动阻塞修复说明

## 现象

Agent 连续多轮只调用工具（`text_editor` / `bash`，模型没有产出任何文本
增量）时，Telegram 客户端里的草稿会长时间（观察到可达数分钟）卡在原地
不动，之后才突然跳到最新状态，容易让人误以为 Agent 已经卡死。

## 根因

草稿达到滚动阈值（累计字符数或结构块数超限）时，`RichMessageBuilder.
_rollover_draft_if_needed()` 会先把已完成的内容通过 `send_rich_html_message()`
发成一条**永久消息**，再切换到一个新的 `draft_id` 继续输出剩余内容。

问题出在两层：

1. **`send_rich_html_message()` 没有设置超时。** 它内部用的是
   `aiohttp.ClientSession()`（无 `timeout` 参数，等于 aiohttp 默认的
   几分钟级超时），外面还包了 `retry_async(max_retries=3, delay=1,
   backoff=3, ...)`。一旦网络抖动或 Telegram 侧偶发变慢，单次调用可能
   挂很久，三次重试更是雪上加霜。相比之下，草稿本身的
   `send_rich_message_draft()` 早就改成了 5 秒总超时 + 最多 2 次重试
   （见 `BASH_HANG_FIX.md` 同批次的相关优化），但这次优化没有覆盖到
   "滚动时触发的永久消息"这条路径。

2. **这次慢请求发生在 `flush()` 的同步调用链上。** 旧版
   `flush()` 是 `await self._rollover_draft_if_needed(...)`——即使
   `_rollover_draft_if_needed` 内部把网络 I/O 放到它自己的
   `_rollover_lock` 之外，`flush()` 这一次调用本身仍然会在这一行原地
   阻塞，直到永久消息真正发送完成。而草稿的两条刷新链路
   （`RichMessageBuilder._stream_flush_loop` 的静默保活循环、
   `get_ai_response` 里工具执行期间的 `refresh_loop`）都是"上一次
   flush() 完全返回，才发起下一次"的顺序循环。于是单次慢速的永久消息
   发送，会把整条刷新链路一起拖住——前端表现为草稿冻结，直到那次网络
   调用结束为止。

## 修复

### 1. `send_rich_html_message` 加有界超时

`src/apitelegramchat/utils.py`：给内部的 `aiohttp.ClientSession` 加上
`timeout = aiohttp.ClientTimeout(total=15, connect=5)`。仍然保留原有的
3 次重试（永久消息需要比草稿更强的送达可靠性），但把"无上限"收窄到
"最坏情况约 45~90 秒（含退避）"，而不是可能无限拖长。

### 2. 滚动的网络 I/O 从"调用方等待"改为"独立后台任务"

`src/apitelegramchat/ai_handlers.py`：

- 新增 `RichMessageBuilder._maybe_start_rollover(html_content)`：只做
  一件事——检测到需要滚动时，把 `_rollover_draft_if_needed(...)` 包装成
  `asyncio.create_task(...)` 扔到后台跑，**不 await 它**。同一时刻已有
  一个滚动任务在跑时不会重复创建。
- `flush()` 里原来 `await self._rollover_draft_if_needed(...)` 的地方
  改成同步调用 `self._maybe_start_rollover(...)`，`flush()` 立刻继续走
  自己的逻辑（在 `_flush_lock` 内重新读取当前状态、发送这一帧），不再
  等滚动完成。
- `_rollover_draft_if_needed()` 内部结构相应调整：
  - 状态判断 / 切分 / 内存重建仍然放在 `self._rollover_lock` 临界区内
    （必须与其他调用互斥，但都是纯内存操作，很快）。
  - 网络 I/O（`send_rich_html_message` 发永久消息、`mark_draft_dead`、
    `delete_message` 删旧草稿预览）被移到该临界区**之外**执行。
  - 网络 I/O 完成、真正切换 `draft_id` 后，因为调用方（`flush()`）已经
    不再等待这里执行完，`_rollover_draft_if_needed` 会自己
    `await self.flush(force=True)` 发一次新草稿首帧，不能再假设"调用
    方会在这之后接着发"。
  - 极端并发下（两次判断几乎同时命中阈值），网络 I/O 完成后重新拿锁时
    会检查 `self.draft_id` 是否仍等于本次调用开始时的 `old_draft_id`，
    不一致则跳过重复切换（已发的永久消息内容依然正确落地，不会丢）。
- `stop_flush_loop()`：新增对 `self._rollover_task` 的处理。它的语义和
  另外两个刷新任务不同——承载的是"已经判定要永久化但还没发完"的内容，
  取消它会直接丢失这段内容，比多等待几秒更糟。因此**不 cancel**，只是
  限时（0.5s）等待；仍未完成则转入后台继续跑完（`_swallow_flush_task`），
  不阻塞收尾流程，也不会把这段内容丢掉。

修复后，滚动触发的永久消息网络调用无论耗时多久，都不会阻塞
`_stream_flush_loop` / `refresh_loop` 的后续刷新——它们该多久刷一次
还是多久刷一次。

## 验证

代码改动已通过：

```bash
python3 -m py_compile \
  src/apitelegramchat/ai_handlers.py \
  src/apitelegramchat/utils.py \
  src/apitelegramchat/tool_executors.py \
  tests/validate_draft_rollover.py
```

`tests/validate_draft_rollover.py` 中与滚动相关的测试已同步更新以反映
新的异步时序（滚动不再在 `flush()` 返回前同步完成，需要显式
`await builder._rollover_task`），并新增
`test_rollover_network_io_does_not_block_flush`：mock 一次耗时 0.3s 的
永久消息发送，断言 `flush()` 调用本身在 0.2s 内返回、且滚动进行中另一次
并发 `flush()` 调用同样不被阻塞。

由于本次开发环境无法访问外网安装 `openai` / `PIL` / `cachetools` 等
`ai_handlers.py` 的顶层依赖，无法在此环境内实际跑通完整测试套件（仅做了
`ast` 级别的语法与结构静态检查，以及针对核心时序属性的独立纯逻辑仿真，
仿真结果见下）。**建议在有完整依赖的真实环境中执行一次
`python3 tests/validate_draft_rollover.py` 作为最终确认。**

### 独立仿真验证（不依赖项目内部实现，只验证算法本身）

用一段最小化的 asyncio 脚本复刻 `_stream_flush_loop` / `refresh_loop` /
`flush()` / 滚动后台任务的调用关系和加锁方式，模拟"滚动触发的永久消息
耗时 20 秒"这一场景：

- 修复前的结构（`flush()` 同步 `await` 滚动）：`_stream_flush_loop` 在
  30 秒内只成功 flush 了 4 次，最大间隔 22 秒——完全复现了"卡住一会儿"
  的现象。
- 修复后的结构（滚动作为独立后台任务，`flush()` 不等待）：同样场景下
  `_stream_flush_loop` 在 30 秒内 flush 了 14 次，最大间隔 2.06 秒，
  与没有滚动发生时的正常节奏一致。


## 第二次修复：达到阈值后立即切换草稿

2026-08-13：进一步修复“达到滚动阈值后仍等待永久消息网络请求”的问题。旧实现虽然把 rollover 放入后台任务，但永久消息发送仍使用当前草稿的串行化发送锁，因此慢网络会让后续 draft flush 排队。现在 rollover 的顺序改为：先标死旧 draft、立即创建并登记新 draft、立即发送新 draft 首帧；旧段永久消息和旧预览删除在独立后台任务执行。

因此永久消息接口慢、超时或重试不会再阻塞新草稿刷新。新增回归测试 `test_rollover_switches_draft_before_slow_permanent_send` 覆盖这一属性。
