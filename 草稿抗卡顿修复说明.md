# Telegram 草稿抗卡顿修复说明

## 真实根因：不是块漏算，而是草稿帧过载

用户提供的第 14 轮附近日志已经确认，草稿卡住的主因不是 HTML 代码里的块被漏算，也不是 20,000 字符阈值没有生效。此时草稿仍只有约 15,640–20,329 个可见字符和 150–216 个结构块，均未触及 20,000/280 的主动分段线；但后台仍在高频地发送几乎完整相同的草稿帧。

在日志的第 171–267 帧窗口中，70.282 秒内共生成 97 帧，即每秒 1.380 帧；其中 66 帧实际经历约 898ms 的网络发送，37 帧为 `force=True`。这意味着网络请求与 Telegram 客户端重绘几乎持续占满：上一帧接近完成时，下一帧又开始发送。用户看到的“卡住”实际是客户端被大量全量 Rich Message 更新淹没，无法及时呈现真正的新进度。

| 观测项 | 日志结果 | 含义 |
|---|---:|---|
| 观察窗口 | 70.282 秒 | 第 14–18 轮附近的连续工具链。 |
| 草稿帧数 | 97 帧 | 频率过高，不适合全量富消息重绘。 |
| 实际网络型帧 | 66 帧 | 不是单纯本地缓存命中。 |
| 强制帧 | 37 帧 | 高比例绕过内容去重，重复提交相同或近似内容。 |
| 网络型帧平均耗时 | 898.1ms | 发送几乎背靠背，客户端没有喘息空间。 |

## 修复内容

| 修改 | 新策略 | 目的 |
|---|---|---|
| Bash / 子代理进度回调 | 不再对每条进度 `force=True` 并同步等待发送；更新预览后由合并式刷新处理，主动排队最多每秒一帧。 | 保留最新进度，同时避免工具 stdout 高频更新把草稿发送队列塞满。 |
| 静默心跳 | `STREAM_SILENT_FORCE_FLUSH` 默认从 **2 秒**调整为 **8 秒**。 | 静默心跳只确认草稿活跃，不能反复重绘相同内容。 |
| 在途帧合并 | 发送期间的多次更新只保留最新版本；当前帧完成后最多补发一个最新帧。 | 防止 10 条进度变成 10 次完整 Rich Message 发送。 |
| 版本化 dirty 状态 | 仅在内容版本未变化时清除待发送标记。 | 不漏掉在途发送期间的新工具结果，也避免显式刷新后的重复帧。 |
| 20,000 / 280 主动分段 | 草稿达到 20,000 可见字符或 280 块后，在本工具批次结束、下一模型请求前滚动。 | 防止长工具链把单一草稿继续堆积到 API 容量边界。 |
| 帧级日志 | 输出帧序号、字符数、块数、耗时、强制标记和发送结果。 | 若仍出现卡顿，可明确定位是服务端发送、限流还是客户端渲染。 |

> 这次的核心变化是：**降低全量草稿帧的发送频率，而不是再盲目降低字符阈值。**20,000 保持不变；真正解决第 14 轮卡顿的是去掉每秒多次的重复强刷。

## 验证

新增的高频预览回归测试模拟单个 Bash 工具在一次网络发送期间连续上报 12 条进度。修复后只发送：一帧初始状态 + 一帧最终最新状态，而不是为 12 条进度各发一帧。草稿滚动、在途刷新、中断恢复和前端流程回归均已通过。

```text
PYTHONPATH=src python3 tests/validate_draft_rollover.py
PYTHONPATH=src python3 tests/validate_draft_interrupt_paths.py
PYTHONPATH=src python3 tests/validate_draft_frontend_flow.py
```

## 部署后应观察的日志

正常的长工具链中，`草稿帧完成`不应再在每秒连续出现多次 `force=True` 的完整帧。Bash 或子代理的频繁 stdout 应被合并，最新预览仍会持续显示；达到 20,000/280 后会出现 `草稿交互容量预警，下一完整回合边界滚动`，随后在当前批次结束时出现 `草稿已在回合边界滚动`。
