# apitelegramchat 2.2.1 修复说明

本版本针对代码审查中确认的运行时缺陷、状态泄漏风险、部署配置漂移和重复编排逻辑进行了集中修复。修复工作保留原有的 Telegram、MCP、模型调用和工作区接口，重点提高重启后的行为一致性、长期运行稳定性以及运维可追溯性。

## 关键修复

| 范畴 | 修复内容 | 结果 |
| --- | --- | --- |
| 授权白名单 | 新增 `authorization.py`，将白名单从工作目录迁移到 `APITELEGRAMCHAT_DATA_DIR/authorized_users.json`；采用 schema 校验、原子替换和 `0600` 文件权限。 | 容器重启、代码目录只读或工作目录切换时，授权状态不再丢失。 |
| 写入一致性 | `/adduser` 与 `/deluser` 在持久化失败时回滚内存集合。 | 避免“当前进程显示已授权、重启后却失效”的不一致状态。 |
| Webhook 安全 | 移除 URL 查询参数中的 token；改用 Telegram `secret_token` 和 `X-Telegram-Bot-Api-Secret-Token` 请求头的恒定时间比较。 | 机密不再进入 URL、反向代理访问日志或监控日志。 |
| Webhook 运维 | 新增 `apitelegramchat-register-webhook` 命令；默认不丢弃积压 update。 | 注册动作可显式执行、可审计，重启不会隐式清空消息。 |
| 进程状态 | 为 update 去重、临时消息标记、媒体组、会话模型与草稿状态加入 TTL 和惰性回收。 | 长期运行时，内存不会因历史 update、旧会话和临时标记持续无界增长。 |
| Bash 会话 | 持久 Bash 会话加入可配置的空闲回收，默认 30 分钟。 | 高基数用户或命名空间不再永久占用 shell 子进程。 |
| 主消息流 | 合并文本、图片、文档、音频、视频的重复 AI 请求编排。 | 预检、模型调用、历史更新、异常反馈实现集中维护。 |
| 输出安全 | 结构化工具结果与媒体结果中的 URL 进行协议白名单、长度/空白校验与 HTML 属性转义。 | 降低上游数据直接拼入富文本属性时的注入风险。 |
| 配置可靠性 | 关键整数环境变量支持边界校验和安全默认值；移除不再执行的环境清洗残留代码。 | 错误环境变量不会轻易阻断模块导入，安全策略也更清晰。 |
| 构建依赖 | 将运行时直接使用的 `httpx` 声明至 `pyproject.toml`，并提供 webhook CLI entry point。 | 打包安装与源码运行的依赖声明保持一致。 |

## 部署迁移

新部署应设置 `TELEGRAM_WEBHOOK_SECRET_TOKEN`、`WEBHOOK_URL` 与 `APITELEGRAMCHAT_DATA_DIR`。旧的 `WEBHOOK_TOKEN` 仍可作为迁移期回退值，但不再会被拼接进 URL。应用启动后请运行 `apitelegramchat-register-webhook` 注册 Telegram webhook；只有在明确希望丢弃队列历史消息时，才添加 `--drop-pending-updates` 参数。

> `APITELEGRAMCHAT_DATA_DIR` 必须是应用账号私有且可持久化备份的目录。白名单文件会保存在其中，而不是容器镜像或临时工作目录中。

## 验证记录

修复包已执行 Python 源码和测试源码编译、全量 `ruff` 静态检查、无密钥环境下的应用/MCP/Webhook CLI 导入冒烟测试、6 项回归测试以及 `pip wheel . --no-deps` 构建。所有检查均通过。构建产物可由项目根目录执行 `python -m pip wheel . --no-deps --wheel-dir dist` 重现。

## 验证范围说明

本次验证未使用真实 Telegram token、模型 API 密钥、R2 存储或第三方 MCP 凭证，因此没有向外部服务发送请求。上线前应在隔离的预发布环境中完成一次真实 webhook 注册、一次白名单增删重启验证，以及各已启用模型和 MCP 服务的凭证连通性验证。
