# 草稿与长轮次上下文排查笔记

## 官方草稿语义核实

Telegram 的官方 Bot API 将 `sendRichMessageDraft` 定义为流式部分富消息的方法；草稿是临时预览，最终必须以 `sendRichMessage` 持久化。[1]

SDK 文档对同一机制的说明进一步明确：同一个非零 `draft_id` 的更新会原位动画展示；草稿是短暂预览，最终消息需要单独发送。[2]

因此，滚动到新的 `draft_id` 不可能延续前一个草稿的原位动画；新草稿的首帧应立即包含可见的 Thinking 状态，并且旧预览的清理不能阻塞新草稿首帧。

## 当前代码的初步发现

当前 `_rollover_draft_if_needed` 在永久化旧段后按以下顺序执行：标记旧 draft 死亡、`await delete_message(...)` 删除旧预览、创建新 `draft_id`、替换 Builder 尾部内容、返回到 `flush()` 发送新草稿。`delete_message` 具备网络重试，因此它位于新草稿前会直接造成第二个草稿出现偏慢。

当前 `_replace_with_rollover_remainder` 仅把未提交尾部重新写入文本块，没有创建 `tg-thinking` 块，因此新 `draft_id` 的首帧不显示 Thinking。

后续将审阅 `update_conversation_and_ledger`、历史上限和模型循环内的 `loop_messages`，核实“长 Agent 轮次是否整体丢弃上下文”。

## 参考资料

[1]: https://core.telegram.org/bots/api#sendrichmessagedraft "Telegram Bot API — sendRichMessageDraft"
[2]: https://gramio.dev/telegram/methods/sendMessageDraft "GramIO — sendMessageDraft"

## 长轮次上下文压缩研究结论

整轮删除不是唯一方案。Microsoft 的上下文压缩文档把带工具调用的 assistant 消息及对应 tool result 定义为必须一起处理的原子组，同时提供工具结果压缩、滑动窗口和摘要等策略；其核心是按组保留结构有效性，而非按任意消息或整轮粗暴裁剪。[3]

Anthropic 的长时 Agent 实践也指出，compaction 可以避免耗尽上下文窗口，但不足以独立保证跨窗口任务连续性；应为下一段上下文留下结构化进度和关键工作状态。[4]

对本项目而言，安全的优先级应为：

1. 在单轮仍在执行时，保留完整 `loop_messages`，绝不因草稿滚动清理模型上下文；
2. 将历史中的完成工具调用及结果作为完整原子组处理，优先将冗长 result 缩写为可追溯摘要；
3. 保留最近用户目标、最近完成答复、未完成计划/待办及结构化压缩摘要；
4. 只有在达到硬 token 预算后，才从最旧的完整轮次删除，并明确记录删除原因。

## 参考资料补充

[3]: https://learn.microsoft.com/en-us/agent-framework/concepts/agents/conversations/compaction "Microsoft Learn — Compaction"
[4]: https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents "Anthropic — Effective harnesses for long-running agents"
