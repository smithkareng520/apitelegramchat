# Telegram 富消息草稿滚动策略

## 目标与适用范围

本实现解决长篇 AI 富消息在单个草稿中持续累积、接近 Telegram Rich Message 容量限制时可能失败的问题。生成内容的解析后可见文本接近 **30,000 个 Unicode 字符**，或估算的富消息结构块接近 **440 个**时，系统会主动将一个完整的已生成段落永久发送，再创建新的草稿并继续输出未提交内容。该过程对流式输出透明，直到模型生成结束。

Telegram Rich Message 的文本上限为 **32,768 UTF-8 字符**，其中包含自定义 emoji 的替代文本和公式源；同时单条富消息最多容纳 **500 个块**，并包含嵌套块、列表项和表格行。[1] 由于 API 的实际计数包含服务端语义，代码使用解析 HTML 实体后的可见文本作为保守近似，并在主动阈值与真实上限之间保留安全余量。

| 维度 | 真实 API 限制 | 主动滚动阈值 | 实现含义 |
|---|---:|---:|---|
| 富消息文本 | 32,768 | 30,000 | 接近阈值即寻找完整块结尾并永久化该段。 |
| 富消息块 | 500 | 440 | 在达到结构上限之前滚动，避免大量列表项、表格行或嵌套块导致请求失败。 |
| 块闭合后的弹性空间 | 32,512 | 不适用 | 单一完整块刚超过 30,000 时，允许在仍保留 256 字符安全余量的前提下于其闭合处提交。 |

> `sendRichMessageDraft` 是临时预览；需要调用 `sendRichMessage` 才能将生成内容持久化到聊天中。[2] 因而每一次草稿切换都会先成功永久化旧段，失败时不会切换草稿，也不会丢弃旧草稿内容。

## 实现位置

核心实现位于 `src/apitelegramchat/ai_handlers.py`。新增逻辑集中在 `RichMessageBuilder` 和系统提示词两部分。独立回归测试位于项目根目录的 `validate_draft_rollover.py`。

| 组成 | 关键内容 | 职责 |
|---|---|---|
| 配置常量 | `RICH_MESSAGE_TEXT_CHARS_MAX`、`RICH_DRAFT_ROLLOVER_TEXT_CHARS`、`RICH_MESSAGE_BLOCKS_MAX`、`RICH_DRAFT_ROLLOVER_BLOCKS` | 提供可通过环境变量调整的文字和结构预算。 |
| 结构扫描器 | `_scan_rich_html_boundaries` | 解析 HTML 标签、解码实体、估算可见文字，并记录每个最外层完整结构的结束位置。 |
| 边界选择器 | `_pick_rollover_boundary` | 优先选择不超过 30,000 字符和 440 块的最后一个完整块；若无此位置但完整块仍明显低于真实限制，则选择该完整块结尾。 |
| 滚动器 | `_rollover_draft_if_needed` | 永久化旧段、停止旧草稿、删除旧预览、创建并登记新 `draft_id`，再把尾部内容转入新草稿。 |
| 草稿登记 | `_register_active_draft` | 新草稿创建时先登记占位 `message_id=0`，首帧发送成功后登记真实 `message_id`。 |
| 提示词约束 | `build_system_prompt` 的“超长输出的结构化收尾规则” | 引导模型把长回答拆为可独立闭合的表格、折叠块、列表、代码块等同级结构。 |

## 边界选择规则

常规路径只在**完整最外层结构块**之后分段，而不会切开结构内部。项目提示词已允许并明确覆盖段落、标题、代码块、引用块、折叠块、列表、表格、图片/音视频、幻灯片、地图、数学块、页脚等格式；扫描器同样识别这些标签。

| 结构 | 正常行为 | 原因 |
|---|---|---|
| 段落和标题 | 在 `</p>` 或标题闭合后分段 | 保持语义和排版完整。 |
| 折叠块 `details` | 在 `</details>` 后分段 | 不会把 `summary` 与内容拆到不同消息。 |
| 表格 `table` | 在 `</table>` 后分段 | 不会产生半张表；块预算同时统计表格和行。 |
| 列表 `ul`、`ol` | 在列表整体闭合后分段 | 不会截断列表容器或项目结构。 |
| 代码块 `pre` | 在 `</pre>` 后分段 | 避免未闭合代码格式。 |
| 引用、媒体、公式 | 在各自最外层闭合后分段 | 防止 Telegram 富消息渲染结构失效。 |

对于单个不闭合或异常巨大的结构块，系统会先等待其闭合；若可见文本接近真实上限，或估算块数已接近 500，才会采用最后防线：提取可见文本、HTML 转义并按自然空白/句末附近切分为安全段。此降级路径优先保证**不丢内容、不提交超限请求**，但会放弃该异常超长单块的原有复杂格式。正常模型输出不应走此路径。

## 草稿切换生命周期

滚动过程严格按照下列顺序执行，确保已完成内容不会重发、未完成内容也不会丢失。

| 步骤 | 操作 | 一致性保证 |
|---:|---|---|
| 1 | 构建当前 HTML 并移除仅草稿可用的 `tg-thinking`。 | 永久消息不会包含草稿专用占位块。 |
| 2 | 计算解析后文字、嵌套结构块和安全边界。 | 优先在完整块结束位置切换。 |
| 3 | 调用 `sendRichMessage` 永久化旧段。 | 若调用失败，保留当前草稿并等待后续刷新重试。 |
| 4 | 标记旧 `draft_id` 已失效，并清理旧草稿预览消息。 | 防止异步刷新或 UI 中的重复草稿再次出现。 |
| 5 | 生成新的 `draft_id`，登记为当前活跃草稿。 | 活跃状态只指向新草稿，后续永久消息可正确串行化。 |
| 6 | 将未提交尾部写入新构建器，并强制发送新草稿首帧。 | 模型接下来的流式 delta 会继续追加在新草稿上。 |
| 7 | 模型结束后仅发送最后尾段。 | 若尾段为空，跳过最终发送，避免把此前已永久化的完整内容重复发送。 |

## 环境变量

以下环境变量均为可选项。默认值对应项目当前实现；生产环境一般只应在 API 限制变更或监控显示稳定余量后调整。

```bash
RICH_MESSAGE_TEXT_CHARS_MAX=32768
RICH_DRAFT_ROLLOVER_TEXT_CHARS=30000
RICH_MESSAGE_BLOCKS_MAX=500
RICH_DRAFT_ROLLOVER_BLOCKS=440
```

建议不要提高真实上限，也不要把主动阈值设置得过于接近真实上限。文本长度只是本地近似：HTML 标签和属性不计入，但实体、公式源和自定义 emoji 的服务端语义仍可能造成差异。[1]

## 验证

项目新增 `validate_draft_rollover.py`。该测试不调用 Telegram 网络接口，而是替换发送函数后验证状态迁移与发送顺序。执行方式如下。

```bash
python3 validate_draft_rollover.py
python3 -m py_compile src/apitelegramchat/ai_handlers.py validate_draft_rollover.py
```

测试覆盖以下场景。

| 场景 | 验证结果 |
|---|---|
| HTML 实体与表格 | 实体按解码后的可见字符计数；表格行和嵌套块进入结构预算，单元格不会虚增为独立块。 |
| 完整折叠块与表格 | 在 `</details>` 前分段，绝不提交半张表。 |
| 440 块阈值 | 在第 440 个完整块之后滚动。 |
| 稍超 30,000 的完整块 | 在完整块闭合后、真实上限安全余量内提交。 |
| 正常草稿切换 | 旧段先永久化，新 `draft_id` 再发送尾部首帧，活跃草稿登记会从占位 ID 更新为真实消息 ID。 |
| 异常超长单块 | 降级为安全转义文本分段，并验证可见文本完整无丢失。 |

## 参考资料

[1]: https://core.telegram.org/bots/api#richmessage "Telegram Bot API — RichMessage limits"
[2]: https://core.telegram.org/bots/api#sendrichmessagedraft "Telegram Bot API — sendRichMessageDraft"
