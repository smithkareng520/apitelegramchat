# Telegram Rich Message 发送失败修复报告

## 问题结论

本次故障的直接错误是 Telegram 富文本接口返回 `400 Bad Request: RICH_MESSAGE_CONTENT_REQUIRED`。日志显示，草稿在接近容量阈值后触发回合边界滚动，系统尝试将已完成片段永久化；然而永久消息发送失败。随后该片段在后续轮次及最终回复阶段重复发送，仍均失败，最后草稿被标记死亡且最终可见内容未能投递。

| 日志证据 | 含义 |
|---|---|
| 第 20 轮后出现“草稿容量预警” | 草稿达到滚动阈值，开始准备把已完成片段转成永久消息。 |
| 随后 `sendRichHtmlMessage failed: 400 ... RICH_MESSAGE_CONTENT_REQUIRED` | 永久富文本消息的结构不符合 Telegram Rich Message 内容要求。 |
| 后续多次“草稿边界滚动永久化失败，保留当前草稿重试” | 原逻辑正确保留了滚动状态，但没有纠正无效 HTML，因此重试必然失败。 |
| 最终消息再次返回同一 400，且日志记录“不再降级” | 原逻辑没有将失败的富文本转为安全文本；草稿随后仍会进入常规清理，用户可能看不到最终结果。 |

## 根因

富文本构建器此前仅处理了“工具详情完全为空”的情况：为其添加 `<p>` 标签。但是以下同样会被 Telegram 判定为没有有效 Rich Message 内容的情形没有覆盖：

1. `<details>` 的正文是裸文本；
2. `<details>` 的正文只包含 `<i>`、`<b>` 等内联标签；
3. 推理区、工具组说明或工具详情没有任何块级富文本元素。

这些内容在普通 HTML 视图中可显示，却不满足 Telegram Rich Message 对嵌套内容结构的要求。更严重的是，永久消息发送函数对该特定 400 只记录错误并返回失败，不执行结构安全的降级；最终回复分支无论成功与否仍可能删除草稿预览。

## 实施的修复

| 文件 | 修改 | 作用 |
|---|---|---|
| `src/apitelegramchat/ai_handlers.py` | 新增 `_ensure_rich_block_content`，仅在 `details` 内部的裸文本或内联 HTML 没有块级元素时补充 `<p>`。 | 让推理、工具组说明和工具详情形成服务端可接受的 Rich Message 结构，且不改变顶层流式文本的既有行为。 |
| `src/apitelegramchat/utils.py` | 新增 `_rich_message_plain_text_fallback`；永久消息收到 `RICH_MESSAGE_CONTENT_REQUIRED` 时，以提取并转义后的 `<p>` 内容自动重试一次。 | 即使未来存在未知的 HTML 结构组合，也不会因格式问题丢失整条用户回复。 |
| `src/apitelegramchat/ai_handlers.py` | 最终永久消息及其安全回退都失败时，保留最后一帧草稿预览，不执行删除。 | 在网络或服务端异常无法恢复时，最大化保留用户可见处理结果。 |
| `tests/validate_draft_rollover.py` | 覆盖裸文本、内联标签、已有结构化块、工具详情的构建行为。 | 防止嵌套富文本再次退化为不合法结构。 |
| `tests/validate_rich_message_fallback.py` | 模拟首次 400、第二次安全段落发送成功的完整发送路径。 | 验证降级仅执行一次、保留原始首发内容，并返回真正的消息 ID。 |

## 验证结果

已执行源码编译，并运行全部 `tests/validate_*.py` 回归脚本。所有脚本均通过；测试日志中出现的工具异常和工具调用上限信息是相应回归用例刻意模拟的场景，脚本均以 `PASS` 结束。

重点新增验证如下：

| 验证项 | 结果 |
|---|---|
| 工具详情只有内联标签时自动补齐段落 | 通过 |
| 已有表格等块级结构时不重复包装 | 通过 |
| 顶层流式文本与工具 XML 撤回流程保持兼容 | 通过 |
| 永久消息首发收到 `RICH_MESSAGE_CONTENT_REQUIRED` 后，安全段落回退成功 | 通过 |
| 草稿容量滚动、交接内容保留、失败重试和旧预览异步清理 | 通过 |

## 部署建议

直接用压缩包覆盖部署源码后按现有方式重新构建并重启服务即可。建议上线后关注日志中的 `retrying once with a safe paragraph fallback`：该记录表示新兜底成功保护了用户输出；若它频繁出现，说明上游模型或工具仍在持续生成结构不完整的富文本，应再收紧对应提示词或输出格式化逻辑。
