# 热修复记录（2026-09-08）

## 故障现象（生产日志）

图像生成工具链路连续失败：

1. **Round 1**：`generate_image_from_text` 成功出图后，在 R2 上传环节崩溃——
   `NameError: name '_upload_generated_images_to_r2' is not defined`
   （`search/media_tools.py:136`）。**图片已生成但整次结果丢失**，
   模型只收到兜底文案「⚠️ 工具执行出错」。
2. **Round 2**：模型重试 → OpenRouter 先返回 404
   `No endpoints found that support the requested output modalities: image, text`
   → 代码自动降级 image-only 重试 → 撞上 403
   `Key limit exceeded (monthly limit)`（OpenRouter 密钥月度配额耗尽）。
3. **Round 3**：模型把配额错误当成临时故障，**又连发 3 次并行调用**，全部 403，
   白白浪费工具轮次。

## 修复内容（3 个文件，4 处改动）

### Fix 1（致命）— `src/search/media_tools.py`：补上缺失的 import

`execute_generate_image` 调用 `_upload_generated_images_to_r2`（该函数已收敛到
`ai.media_generation`），但收敛时只改了注释、**没有加 import**。补上：

```python
from ai.media_generation import _upload_generated_images_to_r2
```

### Fix 2（鲁棒性）— `src/ai/media_generation.py`：加固 chat modalities 降级链路

`_request_chat_modalities_image_task` 的 image-only 降级逻辑三处加固：

- **401/402/403/429 永不降级重试**：鉴权/配额/限流类错误即使错误文本碰巧含
  "modalities" 也直接上抛（避免第二次注定失败的请求，OpenRouter 按请求计费）；
- **降级重试保留 `n` 参数**：旧代码首次请求带 `n`（多图）、降级时丢失，
  多图请求静默退化为单图；
- **降级重试失败抛 `ImageRequestError`**（携带合并详情与状态码），不再裸抛
  SDK 异常——上层 `execute_generate_image` / 原生图像循环能按统一格式呈现，
  模型能从错误里看出"已降级重试过"，从而判断该换模型而非继续重试。

### Fix 3（防重试浪费）— `src/search/media_tools.py`：非重试类错误附加处置指令

新增 `_non_retryable_hint()`，`_format_image_api_error` 按状态码 + 错误关键词
识别四类错误并在工具结果中附加明确指令，让模型立即止损：

| 错误类别 | 判定 | 附加指令 |
|---|---|---|
| 鉴权/配额 | 401/402/403，或 `key limit exceeded` / `monthly limit` / `quota` / `insufficient` / `billing` 等关键词 | 请勿再次调用本工具，直接向用户说明（余额不足/密钥无效） |
| 限流 | 429 或 `rate limit` | 请勿立即重试；稍后单独调用或换模型 |
| 能力不支持 | 404 且含 `modalities` / `no endpoints`（说明内部降级重试已失败过） | 请勿用同一模型重试，改用其他受支持图像模型 |
| 其他 | — | 无附加提示（维持原样） |

### Fix 4（隐藏 bug，Gemini 链路）— `src/ai/gemini_bridge.py`：补上缺失的 `VideoBlock` import

`_blocks_to_gemini_parts` 在 427 行使用 `VideoBlock`，但 import 列表漏了它。
连锁后果：**任何非文本、非图片的内容块（包括语音消息 AudioBlock）进入
Gemini 原生桥接时，走到 `isinstance(block, VideoBlock)` 即抛 NameError，
整个 Gemini 请求崩溃**。已在 import 列表中补上。

## 验证

- pyflakes 全量扫描：0 个 undefined name（修复前 2 个）
- AST 跨模块 import 校验 + compileall：全部通过
- 103 个模块运行时导入：全部通过
- 定向回归测试（scripts/test_fixes.py，17 项断言）：全部通过
  - 成功链路：dispatch → 出图 → R2 上传 → 返回链接，不再 NameError
  - 降级链路：404 → image-only 降级成功；404 → 降级仍 403 → ImageRequestError(403)；
    403+modalities → 不重试直接上抛
  - 提示分类：403 配额 / 401 鉴权 / 429 限流 / 404 modalities / 普通 500
  - gemini_bridge：VideoBlock / AudioBlock 不再 NameError
- 项目自带测试套件：与修复前结果完全一致（3 个失败均为预存的测试隔离/
  时序问题，在原始未修改代码上同样失败，与本次修复无关）

## 遗留事项（非代码问题，需要运维处理）

- **OpenRouter 密钥月度配额已耗尽**（403 Key limit exceeded）：需要到
  OpenRouter 控制台充值或更换密钥，代码修复无法替代这一步。
