# 草稿滚动修复说明

## 已修复的行为

草稿不再依赖此前某次 `flush()` 是否成功把 `_rollover_pending` 置为 `True`。现在每一个**完整工具批次结束**或**完整模型回复结束**的既有回合边界，都会重新计算当前草稿的可见字符数和 Rich Block 数。

当容量达到 **30,000 个可见字符**或 **440 个 Rich Block** 时，系统会在该回合边界同步执行以下动作：永久化旧草稿段、生成新的 `draft_id`、登记新草稿，并立即发送新草稿首帧。它不会等待整个 Agent 任务结束，也不会再因为旧草稿的一次刷新被限流而把切换拖到后续若干工具轮次。

| 容量状态 | 修复后的行为 |
|---|---|
| 低于 27,000 字符且低于 380 块 | 正常持续更新当前草稿。 |
| 27,000–29,999 字符，或 380–439 块 | 作为预警区间；继续等待本工具回合结束，但不提前永久化。 |
| 达到 30,000 字符或 440 块 | 本回合结束时立刻切换旧草稿到新草稿。 |
| 已达到正式阈值但没有可安全提交的完整富文本边界 | 立即使用现有的安全纯文本切段，避免继续跨回合积压。 |
| 单独 `done` 工具调用 | 正确关闭已在流式阶段创建的工具组，避免未完成工具组跨回合复用。 |

> 27,000 字符仍保留为提前预警，而不是实际切换线。这样可给正在执行的当前工具批次留出增长空间，避免为了“刚到 30,000 才检测”而在下一回合边界已越过 Telegram 的真实上限。

## 修改文件

| 文件 | 修改内容 |
|---|---|
| `src/apitelegramchat/ai_handlers.py` | `rollover_at_turn_boundary()` 在每个回合边界重新统计容量；达到 30,000/440 即切换，不再依赖旧 pending 标记。 |
| `src/apitelegramchat/ai_handlers.py` | 修复 `done`-only 批次的提前返回：在返回前关闭工具组并刷新。 |
| `tests/validate_draft_rollover.py` | 新增限流期间到达 30,000 后仍在边界切换、27,000 仅预警、`done`-only 收尾三组回归测试。 |

## 已完成验证

以下验证均已通过：

```text
PYTHONPATH=src python3 tests/validate_draft_rollover.py
PYTHONPATH=src python3 tests/validate_draft_interrupt_paths.py
PYTHONPATH=src python3 tests/validate_draft_frontend_flow.py
python3 -m py_compile src/apitelegramchat/ai_handlers.py tests/validate_draft_rollover.py
```

其中关键的新回归场景确认：即使旧草稿在 429 冷却期内导致 `flush()` 没有设置 pending，只要草稿在完整工具回合结束时达到 30,000 字符，仍会永久化旧段、创建新 `draft_id` 并发送新草稿首帧。
