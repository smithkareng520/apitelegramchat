# Telegram 草稿持续刷新与主动分段修复说明

## 这次解决什么问题

此前草稿在长工具链的中段可能数轮不动，直到接近 API 容量上限才重新出现容量预警或新的草稿。该现象会让用户感觉后端仍在执行，但 Telegram 界面卡在旧状态。

本次不再把“草稿是否流畅”建立在 27,000 或 30,000 字符的 API 容量阈值上，而是采用**低容量主动分段 + 在途更新可靠补发 + 帧级诊断**三层策略。

| 机制 | 修复后的行为 | 解决的问题 |
|---|---|---|
| 交互性能阈值 | 草稿达到 **15,000 个可见字符**或 **160 个 Rich Block** 后，在紧随其后的完整工具批次结束、下一次模型请求前分段并新建草稿。 | 不再让单一草稿在第 12 至第 17 轮持续膨胀到客户端难以重绘的体量。 |
| API 容量保护 | 27,000/380、30,000/440 与 32,768/500 仍保留，作为额外安全保护和富文本边界选择。 | 避免 Telegram Rich Message 的真实限制。 |
| 在途刷新补发 | 草稿帧网络发送期间出现的工具结果或流式文本会记录为新版本；当前帧完成后立即补发最新帧。 | 避免新增状态被已有刷新任务合并后却没有第二帧可见。 |
| 帧级诊断 | 每次草稿发送都会记录 `draft_id`、序号、可见字符数、块数、耗时与结果；限流跳过也单独记录。 | 下一次若仍卡住，可从日志精确判断是内容未变化、发送跳过、限流、网络失败还是客户端未更新。 |

> 预期交互：第 N 轮中草稿达到 15,000/160 后，第 N 轮工具批次结束时即永久化旧段并发送新草稿首帧；绝不会等待第 N+1 轮模型请求，更不会继续等待 27,000 或 30,000。

## 新增配置

默认值适合工具调用频繁、需要持续可见进度的场景；如需更短或更长的草稿段，可通过环境变量调整。

```bash
RICH_DRAFT_INTERACTIVE_TEXT_CHARS=15000
RICH_DRAFT_INTERACTIVE_BLOCKS=160
```

阈值越低，界面越流畅、草稿段越短；阈值越高，单段信息更多，但长工具链中的客户端重绘压力也更大。

## 关键日志

部署后可按下列日志判断运行状态：

| 日志片段 | 含义 |
|---|---|
| `草稿帧完成: ... seq=... result=... chars=... blocks=... elapsed_ms=...` | 一帧草稿已完成发送调用；可观察每轮是否有持续帧以及耗时。 |
| `草稿帧跳过（本地限流冷却）` | 本地正在等待 429 冷却；包含剩余等待毫秒。 |
| `草稿交互容量预警，下一完整回合边界滚动` | 已达到 15,000/160，当前工具批次结束后必换草稿。 |
| `草稿已在回合边界滚动` | 旧草稿已永久化，新 `draft_id` 已创建并发送首帧。 |

## 修改文件

| 文件 | 修改内容 |
|---|---|
| `src/apitelegramchat/ai_handlers.py` | 新增 15,000 字符 / 160 块交互性能阈值；在下一完整工具回合边界主动分段。 |
| `src/apitelegramchat/ai_handlers.py` | 使用版本化 dirty 状态，保证在途草稿发送期间的新内容必定补发，同时避免显式刷新后的无意义重复帧。 |
| `src/apitelegramchat/ai_handlers.py` | 新增草稿帧完成、限流跳过与容量预警诊断日志。 |
| `tests/validate_draft_rollover.py` | 覆盖 15,000 提前分段、在途发送补发最新内容、容量边界、限流和 `done`-only 收尾。 |

## 已完成验证

以下检查已通过：

```text
PYTHONPATH=src python3 tests/validate_draft_rollover.py
PYTHONPATH=src python3 tests/validate_draft_interrupt_paths.py
PYTHONPATH=src python3 tests/validate_draft_frontend_flow.py
python3 -m py_compile src/apitelegramchat/ai_handlers.py tests/validate_draft_rollover.py
```
