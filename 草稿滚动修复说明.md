# 草稿滚动修复说明

## 已修复的行为

草稿不再依赖此前某次 `flush()` 是否成功把 `_rollover_pending` 置为 `True`。现在每一个**完整工具批次结束**或**完整模型回复结束**的既有回合边界，都会重新计算当前草稿的可见字符数和 Rich Block 数。

当容量达到 **27,000 个可见字符**或 **380 个 Rich Block** 的预警线时，系统会在**紧随其后的完整工具批次结束、下一次模型请求开始之前**同步执行以下动作：永久化旧草稿段、生成新的 `draft_id`、登记新草稿，并立即发送新草稿首帧。它不会等待整个 Agent 任务结束，也不会继续等到 30,000/440，更不会因为旧草稿的一次刷新被限流而把切换拖到后续若干工具轮次。

| 容量状态 | 修复后的行为 |
|---|---|
| 低于 27,000 字符且低于 380 块 | 正常持续更新当前草稿。 |
| 达到 27,000 字符或 380 块 | 标记预警；紧随其后的完整工具回合结束时立刻切换旧草稿到新草稿。 |
| 30,000 字符或 440 块 | 正常情况下不会再等到这一线；它们仅保留为富文本边界选择与极端内容的安全余量。 |
| 已达到正式阈值但没有可安全提交的完整富文本边界 | 立即使用现有的安全纯文本切段，避免继续跨回合积压。 |
| 单独 `done` 工具调用 | 正确关闭已在流式阶段创建的工具组，避免未完成工具组跨回合复用。 |

> 27,000 字符既是预警线，也是**下一完整工具回合边界的切换线**。它仍为当前正在执行的工具批次保留增长空间，但不会让用户再等待额外的若干模型/工具轮次。

## 持续刷新链路的补充修复

此次根据“第 12 轮后草稿不动、预警到第 17 轮才出现”的现象，额外修复了一个独立于阈值判断的刷新合并缺口：旧的 `request_flush()` 在已有草稿发送任务运行时直接返回。若工具状态或流式文本恰好在该网络发送期间继续变化，而之后没有新的刷新请求，这些新增状态会被合并在内存中却没有新的草稿帧发出，客户端就会表现为长时间停在旧内容。

现在构建器使用 `_flush_dirty` 跟踪发送期间的新变化。每次在途发送结束后，若内容在发送过程中继续变化，就立即补发一帧包含最新工具结果和流式文本的草稿；不会依赖下一轮模型调用、容量预警或静默心跳来“碰巧”刷新。

| 场景 | 修复前 | 修复后 |
|---|---|---|
| 一帧草稿正在网络发送，工具结果/流式文本又到达 | 新的 `request_flush()` 被已有任务合并，可能没有第二帧。 | 标记为 dirty；第一帧结束后立即补发最新完整草稿。 |
| 第 12 轮后连续工具更新 | 可能直到后续新事件或容量预警才看见更新。 | 每个在途发送期间产生的最新状态都会被补发。 |
| 到达 27,000 / 380 预警 | 受刷新时机影响，可能很晚才发现。 | 后续草稿帧持续统计；预警后下一个完整回合边界立即切换。 |

## 修改文件

| 文件 | 修改内容 |
|---|---|
| `src/apitelegramchat/ai_handlers.py` | `rollover_at_turn_boundary()` 在每个回合边界重新统计容量；达到 27,000/380 预警即在该边界切换，不再依赖旧 pending 标记。 |
| `src/apitelegramchat/ai_handlers.py` | 修复 `done`-only 批次的提前返回：在返回前关闭工具组并刷新。 |
| `tests/validate_draft_rollover.py` | 新增限流期间的边界切换、27,000 预警后边界切换、在途发送期间追加内容必须补发、`done`-only 收尾等回归测试。 |

## 已完成验证

以下验证均已通过：

```text
PYTHONPATH=src python3 tests/validate_draft_rollover.py
PYTHONPATH=src python3 tests/validate_draft_interrupt_paths.py
PYTHONPATH=src python3 tests/validate_draft_frontend_flow.py
python3 -m py_compile src/apitelegramchat/ai_handlers.py tests/validate_draft_rollover.py
```

其中关键的新回归场景确认：即使旧草稿在 429 冷却期内导致 `flush()` 没有设置 pending，只要草稿在完整工具回合结束时达到 27,000 字符或 380 块，仍会永久化旧段、创建新 `draft_id` 并发送新草稿首帧。
