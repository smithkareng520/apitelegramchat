# 原始 DOM 表格回填：安全设计

本机制不替换 Trafilatura 的正文清洗，也不扫描或任意插入网页中的表格。它只处理一个狭窄且可验证的失败模式：**原始 DOM 与已提取的同一张表可匹配，但已提取版本明显缺失数据行**。

| 防护层 | 必须满足的条件 | 目的 |
|---|---|---|
| 原始表资格 | 表格至少有 4 个结构行、至少 3 个非表头数据行，且存在至少 3 个非空表头单元格。 | 排除布局表、导航表和一行信息卡。 |
| 同表匹配 | 原始表与已提取的 Rich HTML 表格具有相同的前三个归一化表头，且首个非空数据行的首列一致。 | 只允许用原始表替换已被识别为同一对象的转换表，不会新增无锚点表格。 |
| 丢失判定 | 原始表的结构行数至少比已提取表多 3 行，且至少为后者的 2 倍。 | 防止对正常、仅有轻微格式差异的表格触发回填。 |
| 输入清洗 | 单元格只使用原始 DOM 的可见文本；所有文本经 HTML 转义；仅保留已验证为 2–20 的 `rowspan` / `colspan`。 | 避免将网页中的任意标签、脚本、属性或事件处理器带进模型可见 HTML。 |
| 输出限制 | 回填表格仍是现有正文块，继续受 `FETCH_BODY_TOKEN_BUDGET`、`FETCH_RESPONSE_TOKEN_BUDGET` 与整块截断规则约束。 | 不绕开 token 预算或放大上下文风险。 |
| 失败策略 | 任一步解析、匹配或安全检查失败时，保持原有 Trafilatura 结果，不回填。 | 默认不改变现有页面行为。 |

该规则只解决“已识别同表、但转换结果严重缺行”的问题。它不声称能修复所有页面，因而不会因追求通用性而扩大行为面。每个回填必须在测试中验证：完整表恢复、完整表不替换、无匹配表不插入，以及危险 HTML 不被保留。
