# 草稿前端响应优化说明

## 目标与结论

本次优化聚焦 Telegram 富消息草稿在 Agent 执行过程中的**可见反馈时机、刷新可靠性与渲染负载**。原实现已具备草稿滚动、取消串行化和工具进度展示能力，但在「构建上下文」「解析附件」「网络发送抖动」和「工具返回内容较大」等场景下，用户会长时间只看到过期草稿，或误判 Agent 已停滞。

优化后，草稿会在准备系统提示词、读取用户输入之前立即出现；状态更新不会被最小刷新间隔直接丢弃；单帧网络失败不再占用发送锁数十秒；工具详情展示被限制在可控预算内。最终答复、工具原始结果、草稿滚动的完整块一致性均保持不变。

> `sendRichMessageDraft` 用于流式临时预览，`sendRichMessage` 才会把内容持久化；因此优化保持“草稿可随时替换、永久消息严格串行”的既有语义。[1]

## 草稿与 Agent 生命周期

| 阶段 | 优化前的用户感知 | 优化后的用户感知 | 一致性处理 |
|---|---|---|---|
| 请求进入 | 需先完成提示词、历史和多模态准备，草稿可能迟迟不出现 | 先显示“正在准备请求…” | 先注册活跃 `draft_id`，首帧成功后登记 `message_id` |
| 上下文准备 | 无法区分正在整理上下文还是卡住 | 显示“正在整理对话上下文…” | 状态仍属于同一草稿，不创建额外消息 |
| 附件/多模态解析 | 解析期间草稿往往停在旧内容 | 显示“正在读取你的输入…” | 解析完成后切换为“正在思考…” |
| 模型流式输出 | 1 秒或 200 字符后刷新；相邻更新可能被直接跳过 | 0.65 秒或 96 字符后刷新；相邻新内容将等待短暂空档后发送 | 同一 `draft_id` 下串行发送，避免并发覆盖 |
| 工具执行 | 大型工具详情被反复完整传输，Telegram 客户端重绘变慢 | 保留工具标题与最新状态，限制详情展示预算 | 模型上下文和最终回复仍保留完整工具结果 |
| 网络短暂失败 | 单帧最多 3 次、每次 12 秒，发送锁可能被占用 38 秒以上 | 单帧最多 2 次、每次 5 秒，短退避后交给后续完整帧恢复 | 429 仍走已有冷却逻辑；永久消息未改为弱一致性 |
| 结束/滚动 | 完整段落永久化，再切换新草稿 | 保持原有行为 | 不切开表格、列表、折叠块或代码块 |

## 已实施的改动

### 1. 将草稿首帧前移

`get_ai_response` 现在在读取会话锁、构建系统提示词、追加历史和解析多模态内容**之前**创建并发送草稿。随后通过 `set_thinking_status` 更新同一占位块，而不是另发临时消息。状态文本经 HTML 转义，避免用户输入或内部状态破坏富消息结构。

这解决了附件处理、长历史归档或技能目录读取期间“无任何前端反馈”的问题。模型开始请求前，用户可稳定看到“正在思考…”。

### 2. 提高刷新频率且允许运行时配置

流式刷新默认值由 `1.0 秒 / 200 字符 / 4.0 秒静默保活` 调整为 `0.65 秒 / 96 字符 / 2.0 秒静默保活`。所有数值均可通过环境变量覆盖：

| 环境变量 | 默认值 | 作用 |
|---|---:|---|
| `STREAM_FLUSH_INTERVAL` | `0.65` | 有新增内容时的最长常规刷新间隔（秒） |
| `STREAM_FLUSH_CHARS` | `96` | 达到约一半即触发刷新判断的字符预算 |
| `STREAM_SILENT_FORCE_FLUSH` | `2.0` | 工具或模型静默期间的草稿保活间隔（秒） |

配置值具有最小下限，避免因错误环境变量把刷新频率设为零或负数。

### 3. 修复最小发送间隔导致的“更新丢帧”

此前，距离上一帧不足 `0.25` 秒的新内容会直接返回；调用方随后清空待发送字符计数，新的状态可能要等到下一次静默保活才出现。现在发送器会在持锁状态下最多等待剩余的 `0.25` 秒空档，然后重新确认草稿仍活跃并发送最新帧。重复内容仍会被缓存跳过。

该策略把“直接丢弃最新 UI 状态”变为“短暂合并后发送最新 UI 状态”，同时维持服务端刷新间隔约束。

### 4. 缩短易被替换草稿帧的网络阻塞

草稿帧属于瞬态界面，后续帧可覆盖早期帧，因此不应把永久消息的重试强度直接用于草稿。`send_rich_message_draft` 现在单次总超时为 5 秒、连接超时为 2.5 秒、最多 2 次尝试、退避为 0.25 秒；草稿 reassert 的超时由 8 秒降至 4 秒。

这将最差瞬态锁占用从约 **38.4 秒**降低到约 **10.25 秒**（两个 5 秒请求加一次 0.25 秒退避），并让后续流式内容更快接管恢复。限流响应仍会抛出既有的 `RateLimitError`，由 Builder 进入冷却期，不会用快速重试放大限流。

### 5. 为工具详情设置草稿 UI 预算

工具返回的原始结果仍完整传给模型和最终答复，但草稿面板不再无限累积每一个工具的详情。`bash` 与 `text_editor` 每项最多展示 6,000 字符，其他工具默认最多 2,400 字符，单个工具组详情总预算为 12,000 字符。超过预算时保留工具标题并显示“工具输出已截断”。

这避免搜索、抓取等工具的大量输出被后续每一帧反复序列化、传输与重绘。与 Telegram 对富消息文本和结构块均有限额的限制相一致，此策略同时降低客户端负担和超限风险。[1]

## 未改变的关键保护

| 保护机制 | 保持方式 |
|---|---|
| 长输出滚动 | 仍在完整最外层结构边界永久化，避免截断表格、折叠块、列表与代码块。 |
| 草稿/永久消息顺序 | 继续使用活动草稿发送锁；永久消息和草稿刷新不会交错。 |
| 取消处理 | 继续等待旧任务与其刷新子任务停止，再发送停止消息或开始新请求。 |
| 限流处理 | 继续识别 Telegram 429 并进入冷却期。 |
| 最终内容 | 草稿详情裁剪只影响临时 UI，模型工具消息和最终富文本不截断。 |

## 验证结果

已完成以下检查：

```bash
python3 -m py_compile \
  src/apitelegramchat/ai_handlers.py \
  src/apitelegramchat/utils.py \
  src/apitelegramchat/config.py \
  validate_draft_rollover.py \
  validate_draft_frontend_flow.py

python3 validate_draft_rollover.py
python3 validate_draft_frontend_flow.py
```

其中，既有 `validate_draft_rollover.py` 通过，确认草稿滚动仍遵守完整结构边界、旧段先永久化、新草稿续写和异常超长块降级等行为。新增 `validate_draft_frontend_flow.py` 通过，覆盖以下内容：

| 新增回归项 | 验证点 |
|---|---|
| 准备状态 | 思考状态正确转义，并发出刷新请求。 |
| 工具详情预算 | 所有工具标题保留；超过预算时出现截断提示；可见详情大小受控。 |
| 首帧时序 | 草稿首帧早于系统提示词构建；附件解析和模型调用前均有对应状态。 |

## 部署建议

直接部署本项目目录即可。若生产环境中 Telegram 侧刷新限制更严格，可只调整上述三个 `STREAM_*` 环境变量；建议先保留本次默认值并观察日志中的 429、超时和草稿失败计数。不要提高富消息真实上限或把草稿滚动阈值贴近上限，以保留服务端计数差异的安全余量。[1]

## 参考资料

[1]: https://core.telegram.org/bots/api#sendrichmessagedraft "Telegram Bot API — sendRichMessageDraft"
