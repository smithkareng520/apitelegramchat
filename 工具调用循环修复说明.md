# 工具调用循环修复说明

## 问题分析

用户提供的日志显示，模型在同一轮任务中累计发起了 40 次工具调用后触发旧限制。此时系统虽然尝试切换到“无工具总结”，但模型在总结响应中仍以普通文本输出了 `<tool_call>` XML。该文本被当作最终回答的一部分写入草稿和永久富消息，导致用户看到了内部工具调用语法，而不是可读的完成状态。

本次修复将**真实执行的工具调用预算**由 40 次提升为 **100 次**，并同时修正超限批次、总结响应和伪工具调用文本的处理。仅提高上限而不处理上述路径，会把相同问题延后到第 100 次调用后，不能真正解决日志中暴露的体验问题。

## 行为变更

| 场景 | 修复前 | 修复后 |
|---|---|---|
| 默认工具调用预算 | 每轮最多 40 次真实工具调用。 | 每轮最多 **100 次**真实工具调用。 |
| 同一批次超出余量 | 可能先执行整批调用，使总数越过上限。 | 仅执行剩余预算允许的调用；其余调用不执行。 |
| 被跳过调用的协议状态 | 可能缺少与 assistant `tool_call_id` 配对的 `tool` 消息。 | 为每个跳过 ID 追加标准 `tool` 消息，明确说明预算已耗尽。 |
| 达到上限后的总结 | 模型可能把 `<tool_call>` XML 作为普通文本输出。 | 总结请求不携带 `tools`，并剥离所有文本伪工具调用；若只剩 XML，则返回安全状态说明。 |
| 流式草稿 | 已流入草稿的原始 XML 可能保留。 | 从最近的文本块或工具组说明中撤回原始 XML，再写入清理后的文字。 |
| 异常模型循环 | 可能一直尝试文本格式工具调用。 | 文本伪调用最多纠正 3 次，之后返回可读提示并停止循环。 |
| 循环自然耗尽 | 可能返回 `None` 或不完整结果。 | 返回无工具、无 XML 的明确状态说明。 |

> 预算按**实际执行数量**累计，而不是按模型轮数累计。模型可在一个响应内请求多个并发工具；当还剩 2 次额度时，系统只执行其中前 2 个，并将剩余调用标记为未执行。

## 实现位置

修改集中在 `src/apitelegramchat/ai_handlers.py`，并新增 `validate_tool_loop.py` 回归测试。

| 组件 | 修改内容 |
|---|---|
| `MAX_TOOL_CALLS` | 从 `40` 调整为 `100`。 |
| `_run_tool_calls_and_append` | 根据剩余预算筛选同批工具；对跳过调用补齐 `tool` 响应；在第 100 次执行后进入无工具总结。 |
| `_contains_textual_tool_call` / `_strip_textual_tool_calls` | 检测并清理 `<tool_call>` 和 `<longcat_tool_call>` 文本 XML。 |
| `RichMessageBuilder.replace_trailing_text` | 从草稿的文本块或工具组说明中撤回已流入的伪工具调用文本。 |
| OpenAI 兼容循环 | 将伪调用清理前置到草稿、历史和最终回复；超限合成返回只保留正常文字。 |
| Gemini 兼容循环 | 同步清理普通内容和超限总结，保持多模型路径一致。 |

## 超限生命周期

当计数达到第 100 次真实工具调用时，系统完成已获准执行的工具并追加结果，然后不再向模型提供工具定义。接着发起一次仅用于总结的请求；系统会过滤该请求中错误出现的文本工具调用，并将安全文本显示给用户。若总结请求失败或只输出 XML，系统使用以下可读兜底状态。

```text
本轮已完成 100 次工具调用，已达到单轮安全上限。我已保留成功结果；如仍需继续执行剩余步骤，请发送“继续”。
```

该机制既允许复杂任务拥有更长的执行链，也防止模型或兼容端在上限边界泄漏内部调用格式。

## 验证

在项目根目录执行以下命令。

```bash
python3 validate_tool_loop.py
python3 validate_draft_rollover.py
python3 -m py_compile src/apitelegramchat/ai_handlers.py validate_tool_loop.py validate_draft_rollover.py
```

`validate_tool_loop.py` 覆盖以下关键场景：文本伪工具调用识别与清理；计数从 98 到 100 时只执行剩余 2 个调用；跳过调用仍获得匹配的 `tool_call_id` 响应；以及模型在无工具总结中再次输出 `<tool_call>` XML 时，最终用户可见内容会回退为安全状态说明。
